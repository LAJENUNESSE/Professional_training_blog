# Blog 系统架构文档

## 📋 项目概览

**Blog** 是一个现代化的全栈博客系统，采用前后端分离架构。

- **后端**: Spring Boot 4.0.1 + Java 21
- **前端**: Vue 3 Monorepo (TypeScript)
- **数据库**: SQLite + Flyway 迁移
- **认证**: JWT + 角色权限控制

## 🏗️ 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
├─────────────────────────────────────────────────────────────┤
│  Web Frontend (Port 3000)    │  Admin Dashboard (Port 3001) │
│  - Vue 3 + Tailwind CSS      │  - Vue 3 + Naive UI          │
│  - Public Blog               │  - Management Interface      │
└──────────────┬────────────────┴──────────────┬──────────────┘
               │                               │
               │    HTTP/REST API              │
               └──────────────┬────────────────┘
                              │
┌─────────────────────────────▼────────────────────────────────┐
│                    Backend API (Port 8080)                   │
├─────────────────────────────────────────────────────────────┤
│  Spring Boot 4.0.1 Application                              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Security Layer (JWT + Spring Security)              │  │
│  │  ┌─────────────────────────────────────────────────┐  │  │
│  │  │  Controllers (Public + Admin)                  │  │  │
│  │  │  ┌───────────────────────────────────────────┐  │  │  │
│  │  │  │  Service Layer (Business Logic)          │  │  │  │
│  │  │  │  ┌─────────────────────────────────────┐  │  │  │  │
│  │  │  │  │  Repository (Data Access)          │  │  │  │  │
│  │  │  │  └─────────────────────────────────────┘  │  │  │  │
│  │  │  └───────────────────────────────────────────┘  │  │  │
│  │  └─────────────────────────────────────────────────┘  │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
                              │
                              │  JDBC
                              │
┌─────────────────────────────▼────────────────────────────────┐
│                    Database Layer                            │
│  SQLite Database (blog.db)                                  │
│  - Flyway Migration Management                              │
│  - JPA/Hibernate ORM                                        │
└─────────────────────────────────────────────────────────────┘
```

## 📁 模块结构

### 后端模块 (`backend/`)

```
backend/src/main/java/com/example/blog/
├── BlogApplication.java          # 应用入口
├── common/                       # 公共组件
│   ├── Result.java              # 统一响应格式
│   └── PageResult.java          # 分页响应
├── config/                       # 配置类
│   ├── SecurityConfig.java      # 安全配置
│   ├── JpaConfig.java           # JPA配置
│   ├── JwtConfig.java           # JWT配置
│   └── WebMvcConfig.java        # Web配置
├── controller/                   # 控制器层
│   ├── AuthController.java      # 认证相关
│   ├── ArticleController.java   # 文章相关
│   ├── CategoryController.java  # 分类相关
│   ├── TagController.java       # 标签相关
│   ├── CommentController.java   # 评论相关
│   ├── SettingController.java   # 设置相关
│   └── admin/                   # 管理后台控制器
│       ├── AdminArticleController.java
│       ├── AdminCategoryController.java
│       ├── AdminTagController.java
│       ├── AdminCommentController.java
│       ├── AdminUserController.java
│       ├── AdminSettingController.java
│       └── AdminUploadController.java
├── dto/                          # 数据传输对象
│   ├── request/                 # 请求DTO
│   │   ├── LoginRequest.java
│   │   ├── RegisterRequest.java
│   │   ├── ArticleRequest.java
│   │   ├── CategoryRequest.java
│   │   ├── TagRequest.java
│   │   ├── CommentRequest.java
│   │   └── SettingRequest.java
│   └── response/                # 响应DTO
│       ├── AuthResponse.java
│       ├── ArticleDTO.java
│       ├── CategoryDTO.java
│       ├── TagDTO.java
│       ├── CommentDTO.java
│       ├── SettingDTO.java
│       ├── UserDTO.java
│       └── UploadResponse.java
├── entity/                       # JPA实体
│   ├── BaseEntity.java          # 基础实体（审计字段）
│   ├── User.java                # 用户
│   ├── Article.java             # 文章
│   ├── ArticleLike.java         # 文章点赞
│   ├── Category.java            # 分类
│   ├── Tag.java                 # 标签
│   ├── Comment.java             # 评论
│   └── Setting.java             # 系统设置
├── exception/                    # 异常处理
│   ├── BusinessException.java   # 业务异常
│   └── GlobalExceptionHandler.java # 全局异常处理器
├── repository/                   # 数据访问层
│   ├── UserRepository.java
│   ├── ArticleRepository.java
│   ├── ArticleLikeRepository.java
│   ├── CategoryRepository.java
│   ├── TagRepository.java
│   ├── CommentRepository.java
│   └── SettingRepository.java
├── security/                     # 安全认证
│   ├── JwtAuthenticationFilter.java  # JWT过滤器
│   ├── JwtTokenProvider.java    # Token提供者
│   ├── CustomUserDetails.java   # 用户详情
│   └── UserDetailsServiceImpl.java # 用户详情服务
└── service/                      # 业务逻辑层
    ├── AuthService.java
    ├── UserService.java
    ├── ArticleService.java
    ├── CategoryService.java
    ├── TagService.java
    ├── CommentService.java
    ├── SettingService.java
    └── FileStorageService.java
```

### 前端模块 (`frontend/packages/`)

```
frontend/packages/
├── shared/                       # 共享库 (@blog/shared)
│   ├── src/
│   │   ├── api/                 # API客户端
│   │   │   ├── request.ts       # Axios实例
│   │   │   ├── auth.ts
│   │   │   ├── article.ts
│   │   │   ├── category.ts
│   │   │   ├── tag.ts
│   │   │   ├── comment.ts
│   │   │   ├── setting.ts
│   │   │   ├── user.ts
│   │   │   └── upload.ts
│   │   ├── types/               # TypeScript类型定义
│   │   │   ├── auth.ts
│   │   │   ├── article.ts
│   │   │   ├── category.ts
│   │   │   ├── tag.ts
│   │   │   ├── comment.ts
│   │   │   ├── setting.ts
│   │   │   └── user.ts
│   │   ├── composables/         # 组合式函数
│   │   ├── utils/               # 工具函数
│   │   └── constants/           # 常量定义
│   └── package.json
├── web/                          # 前台博客 (@blog/web)
│   ├── src/
│   │   ├── main.ts              # 应用入口
│   │   ├── App.vue              # 根组件
│   │   ├── router/              # 路由配置
│   │   ├── stores/              # Pinia状态管理
│   │   ├── views/               # 页面组件
│   │   │   ├── HomeView.vue
│   │   │   ├── ArticleView.vue
│   │   │   ├── CategoryView.vue
│   │   │   ├── TagView.vue
│   │   │   ├── SearchView.vue
│   │   │   ├── LoginView.vue
│   │   │   └── RegisterView.vue
│   │   ├── components/          # 通用组件
│   │   │   ├── ArticleCard.vue
│   │   │   ├── ArticleList.vue
│   │   │   ├── CommentList.vue
│   │   │   └── Pagination.vue
│   │   ├── layouts/             # 布局组件
│   │   │   ├── DefaultLayout.vue
│   │   └── styles/              # 样式文件
│   ├── vite.config.ts
│   └── package.json
└── admin/                        # 后台管理 (@blog/admin)
    ├── src/
    │   ├── main.ts              # 应用入口
    │   ├── App.vue              # 根组件
    │   ├── router/              # 路由配置
    │   ├── stores/              # Pinia状态管理
    │   ├── views/               # 页面组件
    │   │   ├── DashboardView.vue
    │   │   ├── ArticleManageView.vue
    │   │   ├── ArticleEditView.vue
    │   │   ├── CategoryManageView.vue
    │   │   ├── TagManageView.vue
    │   │   ├── CommentManageView.vue
    │   │   ├── UserManageView.vue
    │   │   └── SettingView.vue
    │   ├── components/          # 通用组件
    │   │   ├── ArticleEditor.vue  # ByteMD编辑器
    │   │   ├── DataTable.vue
    │   │   └── UploadDialog.vue
    │   ├── layouts/             # 布局组件
    │   │   └── AdminLayout.vue
    │   └── styles/              # 样式文件
    ├── vite.config.ts
    └── package.json
```

## 🔐 安全架构

### 认证流程
1. 用户登录 → JWT Token生成 (24小时有效期)
2. Token存储 → localStorage
3. 请求拦截 → 添加Bearer Token
4. 后端验证 → JWT过滤器验证
5. 权限检查 → 角色-based访问控制

### 权限层级
- **PUBLIC**: 无需认证 (浏览文章、分类、标签)
- **USER**: 普通用户 (评论、点赞、个人中心)
- **ADMIN**: 管理员 (所有管理功能)

## 📊 数据流

### 文章查询流程
```
用户请求 → Web组件 → API调用 → 共享库API → Axios → 后端代理
    ↓
Spring Controller → ArticleService → ArticleRepository → SQLite
    ↓
DTO转换 → 响应 → 共享库类型 → Vue组件渲染
```

### 用户认证流程
```
登录表单 → AuthView → authAPI.login() → AuthController.login()
    ↓
AuthService → UserRepository查询 → 密码验证 → JWT生成
    ↓
返回Token → 存储localStorage → 后续请求自动携带
```

## 🛠️ 技术栈详情

### 后端
- **框架**: Spring Boot 4.0.1
- **安全**: Spring Security 7 + JWT (jjwt 0.12.6)
- **数据**: JPA + Hibernate + SQLite
- **迁移**: Flyway
- **工具**: Lombok

### 前端
- **框架**: Vue 3.5.13 + TypeScript
- **构建**: Vite 6.0.5
- **状态**: Pinia 2.3.0
- **路由**: Vue Router 4.5.0
- **UI**:
  - Web: Tailwind CSS 3.4.17
  - Admin: Naive UI 2.41.0 + Tailwind CSS
- **编辑器**: ByteMD (Admin)
- **Markdown**: markdown-it + highlight.js

## 🚀 部署架构

### 开发环境
```bash
# 后端
cd backend && ./mvnw spring-boot:run

# 前端 (两个应用)
cd frontend && npm run dev:web    # http://localhost:3000
cd frontend && npm run dev:admin  # http://localhost:3001
```

### 生产环境
```bash
# 1. 构建前端
cd frontend && npm run build

# 2. 复制到后端静态资源
cp -r frontend/packages/web/dist/* backend/src/main/resources/static/
cp -r frontend/packages/admin/dist/* backend/src/main/resources/static/admin/

# 3. 打包后端
cd backend && ./mvnw package -DskipTests

# 4. 运行JAR
java -jar backend/target/blog-0.0.1-SNAPSHOT.jar
```

## 📝 设计模式

1. **Repository Pattern**: 数据访问抽象
2. **Service Pattern**: 业务逻辑分离
3. **DTO Pattern**: 数据传输对象封装
4. **Singleton**: Spring Beans默认单例
5. **Observer**: JPA事件监听
6. **Factory**: Spring Bean工厂
7. **Template Method**: JPA Repository方法

## 🎯 核心特性

- ✅ JWT认证与刷新机制
- ✅ 角色权限控制 (RBAC)
- ✅ 文章点赞系统
- ✅ 评论回复树
- ✅ 文件上传管理
- ✅ 数据库迁移 (Flyway)
- ✅ 响应式设计
- ✅ Markdown编辑器
- ✅ 代码语法高亮
- ✅ 搜索功能
- ✅ 分页查询
- ✅ 审计日志 (创建/更新时间)

---

**文档版本**: v1.0
**最后更新**: 2026-01-04
**维护者**: Blog 开发团队