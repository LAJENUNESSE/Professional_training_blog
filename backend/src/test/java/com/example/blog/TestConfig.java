package com.example.blog;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * 测试配置类
 * 提供测试环境所需的Bean配置
 */
@TestConfiguration
public class TestConfig {

    /**
     * 测试配置类
     * 目前使用内存数据库，不需要额外的Bean配置
     * 可以在这里添加测试专用的Bean
     */
}
